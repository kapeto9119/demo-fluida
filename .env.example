# Environment variables for both local development and Railway deployment
# For local development: Copy this file to .env and adjust values as needed
# For Railway: These will be configured as environment variables in the Railway dashboard

# Database configuration
# Local Development
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=fluida
POSTGRES_PORT=5432

# Railway uses DATABASE_URL format instead of individual credentials
# This will be automatically provided by Railway's Postgres plugin
DATABASE_URL=postgres://postgres:postgres@localhost:5432/fluida

# Backend API configuration
PORT=8080  # Railway will set this automatically
API_VERSION=v1

# Frontend configuration
FRONTEND_PORT=3000  # Railway will set this automatically 
NODE_ENV=production
# For local development with Docker Compose
NEXT_PUBLIC_API_URL=http://localhost:8080/api
# For Railway, set this to the backend service URL
# NEXT_PUBLIC_API_URL=https://your-backend-service.railway.app/api

# Docker Configuration (local development only)
COMPOSE_PROJECT_NAME=fluida 